function changeLanguage(hashId, userNick) {
  // TODO: get user > change language.
  /** should decide: translate all messages again?
   * probably not. maybe?
   */
}

export default changeLanguage;
